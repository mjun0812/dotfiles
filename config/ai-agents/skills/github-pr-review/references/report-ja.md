# <reviewer-name> PR Review

<!--
記述ルール:
- 最終レビューには要修正事項のみを書く（verifier が confirmed と判定した指摘のみ）
- 要修正事項がない場合も見出しを残し中身を「なし」と書く
- 各要修正事項には、問題の主な実害を表す短いカテゴリラベルを付ける
- 要修正事項の各項目には `問題` / `完了条件` を含める
- `問題` には発生条件・原因・具体的な実害をまとめる
- `完了条件` には実装方法ではなく、問題が解消されたと判断できる状態を書く
- Finder と verifier が使用した内部情報の `証拠` は最終レビューに書かない
- CI に失敗がある場合は概要にその旨を1行含める
- 最終レビューに要修正事項以外の指摘セクションは作成しない
- inline comment 化されるのは要修正事項セクションのみ
-->

## 概要

<!-- このプルリクエストの変更内容とレビュー結果を1-4文で要約 -->

## 判定

<!-- APPROVE or REQUEST_CHANGES -->

## 要修正事項

- 1: `ファイルパス:行番号` - **[カテゴリ] 問題の説明**
  - 問題: ...
  - 完了条件: ...

---

Reviewed by <reviewer-name> at `<short-sha>`
